version: '3'
services: 
  arsenal:
    build: ./arsenal
    ports: 
      - "5001:5000"
    environment:
  apache:
    build: ./apache
    ports:
      - "8080:80"
    extra_hosts:
      - "host.docker:host-gateway"
